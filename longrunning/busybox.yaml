      - args:
        - tail
        - -f
        - /dev/null
        image: busybox
        imagePullPolicy: IfNotPresent
        name: busybox
        volumeMounts:
        - mountPath: /config
          name: provider-token


      - args:
        - tail
        - -f
        - /dev/null
        image: busybox
        imagePullPolicy: IfNotPresent
        name: busybox
        volumeMounts:
        - mountPath: /azure
          name: azure-json
